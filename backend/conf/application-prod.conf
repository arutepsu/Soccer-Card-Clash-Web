include "application.conf"

play.filters.hosts.allowed += ".up.railway.app"

db.default.driver = "org.postgresql.Driver"
db.default.url = "jdbc:postgresql://"${?PGHOST}":"${?PGPORT}"/"${?PGDATABASE}
db.default.username = ${?PGUSER}
db.default.password = ${?PGPASSWORD}

db.default.hikaricp.connectionTimeout = 60000
db.default.hikaricp.initializationFailTimeout = -1

play.http.filters = "app.filter.Filters"
play.filters.cors.allowedOrigins = [
  "https://scc-frontend-production-ae0c.up.railway.app"
] # fix later to var
play.filters.cors.allowedHttpHeaders = [
  "Accept",
  "Content-Type",
  "Authorization",
  "Origin",
  "Cache-Control",
  "Pragma"
]
play.filters.cors.allowedHttpMethods = [ "GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS" ]
play.filters.cors.supportsCredentials = true
play.filters.cors.preflightMaxAge = 1h
play.http.session.sameSite = "None"
play.http.session.secure = true
play.evolutions.enabled = true